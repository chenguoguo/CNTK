dataDir: .

tags:
    # running BVT job in 'E' (Examples) leg
    - bvt-e 
    # running Nightly job in 'E' (Examples) leg
    - nightly-e 

testCases:
  PyTest run must finish with error code 0 (outputs __COMPLETED__ in that case):
    patterns:
      - __COMPLETED__

